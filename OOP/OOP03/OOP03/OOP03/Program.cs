namespace OOP03
{
    internal class Program
    {
        static void Main(string[] args)

            // Part01
        {
            #region 1.Write a class named Calculator that contains a method named Add. Overload the Add method to
            // check Class Calculator
            #endregion

            #region 2.Create a class named Rectangle with the following constructors:
            // check Rectangle Class
            #endregion

            #region 3.Define a class Complex Number that represents a complex number with real and imaginary parts
            // check Class Complex
            #endregion

            #region 4.Create a base class named Employee with method Work those prints "Employee is working"
            //Employee emp = new Manager();
            //emp.Work();
            #endregion

            #region 5.Create a base class BaseClass with a virtual method DisplayMessage that prints "Message from BaseClass".
            // Check Base and Derived Classed 
            // override keyword : function in the first appearacne must be non-private and virtual to override it 
            // and in binding the baseclass use the last overrided one
            // new keyword : used to give the function new behavior 
            // and in binding can't be accessed in override function chainning
            #endregion

            // Part02
            // Check Duration Class
            
        }
    }
}
